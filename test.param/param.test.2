. param

param.__parse '
    org "org-name"              =str
    repo=hi "repo-name"
    access                      = public private all
    sort                        =[,] repo time update
    show                        =~[,] (repo)|(time)|(update)
' <<<"--org hi  --access private --sort time,repo --show repo,time"

param.__parse --org hi  --access private --sort time,repo --show repo,time <<<'
    org "org-name"              =str
    repo=hi "repo-name"
    access                      = public private all
    sort                        =[,] repo time update
    show                        =~[,] (repo)|(time)|(update)
'
