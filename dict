# shellcheck shell=bash

if [ -n "${BASH_VERSION}" ]; then
    case "${BASH_VERSION}" in
        3*) xrc std/dict_str    ;;
        *) xrc std/dict_bash4   ;;
    esac
elif [ -n "${ZSH_VERSION}" ]; then
    xrc std/dict_zsh
else
    xrc std/dict_str
fi
