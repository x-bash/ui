
p.:(){
    echo return $ERRRR
}

# too many hacks...
w(){

    # trap 'trap -- "" DEBUG; echo $BASH_COMMAND && return 3 2>/dev/null' DEBUG
    trap 'trap -- "" DEBUG; echo good; ERRRR=1; eval $(eval $BASH_COMMAND)' DEBUG

    p.: 1 2 3
    echo $?

    echo "trap -p DEBUG:"
    trap -p DEBUG
    echo hi
    echo $?
}

