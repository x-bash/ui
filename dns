# shellcheck shell=bash

<<A
macos: https://help.dreamhost.com/hc/en-us/articles/214981288-Flushing-your-DNS-cache-in-Mac-OS-X-and-Linux
Consider change the dns server. Without changing, the dns cache would not flush.
A

dns.reset(){
    :
}

dns.list(){
    :
}

dns.config(){
    :
}
